# --- Ядро обработки данных ---
pandas>=2.2,<3.0
numpy>=1.26,<3.0
openpyxl>=3.1          # чтение исходного .xlsx (Online Retail II)
pyarrow>=15.0          # быстрый parquet для промежуточных данных

# --- Работа с СУБД ---
SQLAlchemy>=2.0,<3.0
PyMySQL>=1.1           # чистый python-драйвер MariaDB/MySQL, без компиляции
python-dotenv>=1.0     # конфиг подключения из .env

# --- Загрузка данных из источника ---
requests>=2.31

# --- Аналитика / статистика ---
scikit-learn>=1.4      # RFM-кластеризация, ABC-вспоминалки

# --- Визуализация ---
plotly>=6.0
kaleido>=1.0           # экспорт plotly в PNG; требует Chrome: `.venv/bin/plotly_get_chrome -y`
matplotlib>=3.8
seaborn>=0.13
jinja2>=3.1            # сборка статического HTML-отчёта

# --- Интерактивный дашборд (демо BI) ---
streamlit>=1.33

# --- Утилиты ---
tabulate>=0.9          # таблицы в консоли и в отчёте
python-docx>=1.1       # генерация отчёта по практике в .docx
